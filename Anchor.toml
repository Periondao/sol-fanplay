[toolchain]

[features]
resolution = true
skip-lint = false

[programs.devnet]
fanplay = "iLEPWontSkuaCDeg8psrxB3mVHAA39fWWRtinSJcrtJ"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha --paths -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[test]
startup_wait = 10000
shutdown_wait = 2000
upgradeable = false
